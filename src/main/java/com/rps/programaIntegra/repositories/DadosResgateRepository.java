package com.rps.programaIntegra.repositories;

import com.rps.programaIntegra.entities.DadosResgate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DadosResgateRepository extends JpaRepository<DadosResgate, Long> {
}
